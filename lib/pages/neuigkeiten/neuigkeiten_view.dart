import 'package:flutter/material.dart';

class NeuigkeitenView extends StatelessWidget {
  const NeuigkeitenView ({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Neuigkeiten View'),
    );
  }
}